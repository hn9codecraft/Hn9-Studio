includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 5

    paths:
        - app
        - database
        - routes
        - config

    # Analyse migrations/anonymous-class closures without noise.
    excludePaths:
        - bootstrap/cache/*
