<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
|
| The API is versioned. Route definitions live in per-version files under
| routes/api/ (e.g. routes/api/v1.php) and are mounted at /api/{version}
| from the `then` closure in bootstrap/app.php.
|
| This file is intentionally left without route definitions so that all
| endpoints are reached through an explicit version prefix.
|
*/
